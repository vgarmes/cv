---
const { title } = Astro.props;
---

<section class="flex min-h-0 flex-col gap-y-3 mb-8 md:mb-12 max-w-2xl mx-auto">
  {
    title && (
      <h2 class="text-xl font-bold underline underline-offset-2 decoration-pink-500">
        {title}
      </h2>
    )
  }
  <slot />
</section>
